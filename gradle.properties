# Gradle 配置
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=false

# 统一编码
file.encoding=UTF-8

# 项目版本 —— 唯一来源。
#
# 发布坐标、CHANGELOG、README 的版本口径都以此为准，不要再往 build.gradle.kts 里硬编码。
# 教训：构建脚本曾写死 "3.8.0"，而项目实际已到 3.17.0，漂移 9 个版本都没人发现 ——
# 因为没有任何机制把版本号和 CHANGELOG / 发布坐标绑在一起。改这里，别处跟着走。
projectVersion=3.22.0